import { Currency, Locale } from "@types";

/**
 * Defaults for currency and locale.
 *
 * @remarks
 * When an initial currency or locale is not provided, the Currencyify instance will use these default values.
 */
export const DEFAULT_CURRENCY: Currency = "USD";
export const DEFAULT_LOCALE: Locale = "en-US";
